# 自动排版系统 - 项目记录

## 项目概述

为电脑针车部门开发自动排版工具，替代文泰雕刻的手动排版流程。

## 核心需求

**分组依据：厚度标注**
- 不同厚度的图形必须分开排版（1.0mm一组、1.5mm一组...）
- 相同标注的同形零件不拆散（每件按最近标注独立归属）
- 部件标签（如"4.5 5 6 7 8"尺寸规格串）→ 忽略，不当厚度
- "暂不"、"no" 标注 → 跳过该部件组

**统一板材 600×850**
- 只用 600×850 一种规格（不再使用 400×850）
- 系统自动计算每种厚度需要几张板材

**输出布局：板子横向排列**
- 所有板材（不分厚度）从左到右依次摆放
- 一行放不下时自动换行
- 每张板上方标 `600*850*厚度`

**保留原始图层和颜色**
- 零件按原始图层（底板/面板/卡板等）输出，保留原色
- 仅板材边框画在厚度图层（1.0mm/1.5mm...）用于区分

## 工作流程

```
导入DXF → 剥离容器/参考框 → 整体识别(外框+内件) → 解析文字标注
→ 按厚度分组(每件就近最近标注) → 自动计算板材 → 列优先排版 → 横向输出
```

## 板材规格

| 尺寸 (mm) | 说明 |
|-----------|------|
| 600 x 850 | 统一使用（边距10mm，间距10mm） |

## 输出布局

同一DXF文件，所有板材横向排列（一行装不下换行）。各厚度组按不同颜色区分，板材边框标 `600*850*厚度`。

## 技术栈

| 组件 | 技术 |
|------|------|
| DXF读写 | ezdxf |
| 排样算法 | 列优先矩形排样 + 多板材装箱 |
| GUI | PyQt5 |
| 可视化 | matplotlib |
| 包含检测 | shapely（bbox嵌套） |

## 当前进度

### 已完成
- ✅ DXF解析（ezdxf读取POLYLINE/LWPOLYLINE/CIRCLE/ARC/LINE/SPLINE/ELLIPSE/INSERT）
- ✅ 容器识别：剥离超大框架防误吞零件（containment_detector）
- ✅ 整体识别：外框+内件合并为一个排版单元（bbox嵌套+最近父级）
- ✅ 文字标注解析：支持纯数字/W×H×T/跳过标注/no（text_parser）
- ✅ 按厚度分组：每件按最近标注独立归属（shape_grouper）
- ✅ 列优先矩形排样（纵向优先填满列再开新列）
- ✅ 多板材自动装箱（统一600×850）
- ✅ DXF写入：横向排列+保留原图层颜色+EXTMIN/MAX修复
- ✅ GUI界面（PyQt5）+ 排版预览
- ✅ 回归诊断脚本（tools/diagnose.py）
- ✅ 端到端测试通过（4个测试文件，0丢件）

### 待优化
- ⬜ "暂不"区域跳过逻辑（目前仅识别标注，未实现区域跳过）
- ⬜ 排版利用率进一步提升（单件独占板的利用率偏低）
- ⬜ 处理INSERT块内文字标注的正确世界坐标变换

## 文件结构

```
自动排版/
├── main.py                 # 主程序入口 + GUI
├── config.py               # 配置文件（板材规格、间距等）
├── geometry_utils.py       # 几何工具函数（MBR、旋转、碰撞）
├── requirements.txt        # 依赖包
├── utils/
│   ├── dxf_parser.py       # DXF解析器（实体提取、INSERT变换）
│   ├── dxf_writer.py       # DXF写入器（横向排列、原层保留、EXT修复）
│   ├── text_parser.py      # 文字标注解析（厚度/跳过/W×H×T）
│   ├── shape_grouper.py    # 厚度分组（每件按最近标注归属）
│   └── containment_detector.py  # 包含关系检测（容器剥离+整体识别）
├── nesting/
│   ├── rect_nesting.py     # 列优先矩形排样算法
│   └── board_optimizer.py  # 多板材装箱算法（统一600×850）
├── gui/                    # GUI模块（预留）
└── tools/
    └── diagnose.py         # 无GUI回归诊断脚本
```

## 关键决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2025-07-28 | 放弃pyautocad COM接口 | AutoCAD 2007兼容性问题，写入失败 |
| 2025-07-28 | 改用ezdxf直接操作DXF | 更稳定可靠，维护活跃 |
| 2025-07-28 | 分组依据定为厚度 | 唯一影响排版的因素 |
| 2025-07-28 | 自动计算板材数量 | 用户无需手动选择，一次排好 |
| 2025-07-28→08-01 | 统一600×850板材 | 用户要求，不再使用400×850 |
| 2025-08-01 | 容器剥离防丢件 | 超大框架bbox会吞噬所有内部零件，导致丢件 |
| 2025-08-01 | 每件按最近标注独立归属 | 同规格但不同厚度的零件不能用多数票强合并 |
| 2025-08-01 | 保留原图层颜色 | 用户要求输出零件颜色与原文件一致 |
| 2025-08-01 | 修复EXTMIN/MAX | ezdxf saveas重置为±1e20，CAD打开后视野异常 |
| 2025-08-01 | 板子横向排列 | 用户要求多张板从左到右排，一行满再换行 |
| 2025-08-01 | 列优先排布 | 优先沿高度方向填满列再开新列 |

## 板材自动计算逻辑

对于每种厚度的图形集合：
1. 只使用 600×850 一种板材
2. 逐张板材排，装不下追加新板材
3. 直到所有该厚度的图形排完
4. 输出时显示每种厚度用了几张板

## 容器识别逻辑

输入文件中可能存在"容器/参考框"（如4874×422的整排容器），这些框会bbox吞噬所有内部零件：
1. 任一边超出板材可用区（580×830）的实体 → 判为容器，剥离其父级资格
2. 剩余零件按bbox嵌套+最近父级合并为"整体单元"
3. 整体单元中，外框+内件（小圆等）作为一件排版
4. 超大零件（>580×830）整体略过不排

## DXF输出修复记录

| 问题 | 根因 | 修复 |
|------|------|------|
| 打开后拖不动/难缩放 | EXTMIN/MAX=±1e20，CAD视野异常 | saveas后纯文本修补EXT值 |
| 图形颜色改变 | 零件强制画到厚度图层 | 复刻源图层表，画到原图层 |
| 27个黄1.0变<10 | 同规格多数票把1.5/2.0吞进1.0 | 改回每件按最近标注独立归属 |
| 板子竖直堆叠 | dxf_writer按厚度内Y竖排 | 改为全部板横向排列换行 |
