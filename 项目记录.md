# 自动排版系统 - 项目记录

## 项目概述

为电脑针车部门开发自动排版工具，替代文泰雕刻的手动排版流程。

## 核心需求

**唯一分组依据：厚度**
- 不同厚度的图形必须分开排版
- 1.0mm的所有图形 → 一起排版
- 1.5mm的所有图形 → 一起排版
- 部件标签（如"4.0 2正 短钉"）→ 忽略，不影响排版
- "暂不"标注 → 跳过该部件组

**一次排版，自动计算板材**
- 系统自动计算每种厚度需要几张板材
- 自动选择400x850和600x850的最优组合
- 用户无需手动选择每组的板材

## 工作流程

```
导入DXF → 解析文字标注 → 按厚度分组 → 自动计算板材需求 → 分组排版 → 输出到同一DXF
```

## 板材规格

| 尺寸 (mm) | 说明 |
|-----------|------|
| 400 x 850 | 窄板 |
| 600 x 850 | 宽板 |

## 输出布局

同一DXF文件，水平排列各厚度组，各组之间间距20mm。

## 技术栈

| 组件 | 技术 |
|------|------|
| DXF读写 | ezdxf |
| 排样算法 | 自定义矩形排样 + 多板材装箱 |
| GUI | PyQt5 |
| 可视化 | matplotlib |

## 当前进度

### 已完成
- ✅ DXF解析（ezdxf读取POLYLINE/LWPOLYLINE等）
- ✅ 矩形排样算法（单板材）
- ✅ DXF写入（坐标重建方式）
- ✅ GUI界面（PyQt5）
- ✅ 端到端测试通过

### 待开发
- ❌ 按厚度分组逻辑
- ❌ 多板材装箱算法（自动计算板材数量）
- ❌ 多组排版输出布局
- ❌ 文字标注解析
- ❌ "暂不"跳过逻辑

## 文件结构

```
自动排版/
├── main.py                 # 主程序入口
├── config.py               # 配置文件
├── utils/
│   ├── dxf_parser.py       # DXF解析器
│   └── dxf_writer.py       # DXF写入器
├── nesting/
│   ├── rect_nesting.py     # 矩形排样算法（单板材）
│   └── board_optimizer.py  # 多板材装箱算法（待开发）
└── requirements.txt        # 依赖包
```

## 关键决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2025-07-28 | 放弃pyautocad COM接口 | AutoCAD 2007兼容性问题，写入失败 |
| 2025-07-28 | 改用ezdxf直接操作DXF | 更稳定可靠，维护活跃 |
| 2025-07-28 | 分组依据定为厚度 | 唯一影响排版的因素 |
| 2025-07-28 | 自动计算板材数量 | 用户无需手动选择，一次排好 |

## 板材自动计算逻辑

对于每种厚度的图形集合：
1. 尝试用最少的600x850板材排完
2. 如果排不下，尝试混用400x850和600x850
3. 选择板材总面积最小的方案
4. 输出时显示每种厚度用了几张什么板材
