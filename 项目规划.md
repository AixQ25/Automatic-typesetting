# 自动排版系统 - 项目规划

## 项目概述

开发AutoCAD插件，实现电脑针车模具的自动排版功能，替代文泰雕刻的手动排版流程。

## 技术方案

**方案A：AutoCAD插件** - 用户在AutoCAD中框选图形，自动排版后写回原文件新图层。

## 工作流程

```
工程师在AutoCAD中框选图形
        ↓
运行插件，选择板材尺寸
        ↓
自动排版（10mm间距，可任意旋转）
        ↓
结果写回原文件新图层"排版结果"
        ↓
导出.dxf → 文泰雕刻 → .nc文件
```

## 板材规格（8种）

| 类型 | 尺寸 (mm) | 厚度 |
|------|-----------|------|
| 400宽 | 400 x 850 | 0.5mm, 1.0mm, 1.5mm |
| 600宽 | 600 x 850 | 1.0mm, 1.5mm, 2.0mm, 3.0mm, 4.0mm |

## 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 语言 | Python 3.10+ | 主要开发语言 |
| CAD控制 | pyautocad | COM接口控制AutoCAD |
| 几何计算 | shapely | 碰撞检测、形状操作 |
| GUI | PyQt5 | 图形界面 |
| 矩形排样 | rectpack | 矩形排样算法 |
| 可视化 | matplotlib | 排版预览 |

## 项目结构

```
自动排版/
├── main.py                     # 程序入口
├── config.py                   # 配置（板材尺寸、间距）
├── requirements.txt            # 依赖包
├── autocad_bridge.py           # AutoCAD COM接口封装
├── geometry_utils.py           # 几何工具函数
├── nesting/
│   ├── __init__.py
│   ├── shape.py                # 图形数据模型
│   ├── rect_nesting.py         # 矩形排样算法
│   ├── irregular_nesting.py    # 不规则图形排样
│   └── collision.py            # 碰撞检测
├── gui/
│   ├── __init__.py
│   ├── main_window.py          # 主窗口
│   └── preview_widget.py       # 排版预览
└── utils/
    ├── __init__.py
    └── dxf_export.py           # DXF导出
```

## 模块设计

### 1. autocad_bridge.py - AutoCAD接口

- 连接已运行的AutoCAD (COM接口)
- 获取用户选中的实体
- 提取实体几何信息（LINE, ARC, CIRCLE, LWPOLYLINE, SPLINE等）
- 在指定图层创建新实体
- 图层管理

### 2. geometry_utils.py - 几何工具

- 计算图形最小外接矩形(MBR)
- 计算图形面积
- 图形旋转（任意角度）
- 图形平移
- 生成shapely对象（用于碰撞检测）

### 3. nesting/shape.py - 图形模型

```python
@dataclass
class Shape:
    entities: List          # AutoCAD实体列表
    polygon: Polygon        # shapely多边形
    mbr: Tuple[float, float]  # 最小外接矩形 (width, height)
    area: float             # 面积
    rotation: float = 0.0   # 旋转角度
    position: Tuple = (0, 0) # 排版位置
```

### 4. nesting/rect_nesting.py - 矩形排样

算法：Best Fit Decreasing
1. 按面积降序排列
2. 找到最低最左位置
3. 考虑旋转（0°, 90°, 180°, 270°）

### 5. nesting/irregular_nesting.py - 不规则排样

算法：遗传算法 + 底部左侧填充(BLF)
1. shapely的buffer()添加间距
2. 计算最小外接矩形
3. 遗传算法优化排列和旋转
4. 碰撞检测使用shapely的intersects()

### 6. gui/main_window.py - 主界面

```
┌─────────────────────────────────────┐
│  自动排版工具                        │
├─────────────────────────────────────┤
│  [框选图形]  [板材: ▼400x850-1.0]   │
│                                     │
│  ┌─────────────────────────────┐    │
│  │     排版预览区域             │    │
│  │     (matplotlib画布)        │    │
│  └─────────────────────────────┘    │
│                                     │
│  间距: [10]mm  利用率: 85.3%        │
│                                     │
│  [执行排版]  [写入CAD]  [取消]      │
└─────────────────────────────────────┘
```

## 实施阶段

### 阶段1：基础框架（第1周）

- 创建项目目录结构
- 安装依赖
- 实现autocad_bridge.py（连接AutoCAD、获取选中实体）
- 实现geometry_utils.py（MBR计算、旋转平移）
- 创建测试脚本

### 阶段2：核心算法（第2-3周）

- 实现shape.py数据模型
- 实现rect_nesting.py（矩形排样）
- 实现irregular_nesting.py（不规则排样）
- 实现collision.py（碰撞检测）

### 阶段3：GUI开发（第4周）

- 实现main_window.py（主窗口）
- 实现preview_widget.py（排版预览）
- 集成排版算法到GUI

### 阶段4：CAD集成（第5周）

- 实现图层管理（创建"排版结果"图层）
- 实现实体复制和变换
- 实现dxf_export.py
- 端到端测试

## 排样算法流程

```
输入：选中实体、板材尺寸、间距(10mm)

1. 提取图形
   └── 遍历实体 → 识别类型 → 提取坐标 → 生成shapely对象

2. 预处理
   └── 计算面积 → 计算MBR → buffer(5mm) → 按面积排序

3. 排样
   ├── 全矩形 → rectpack Best Fit
   └── 含不规则 → 遗传算法 + BLF

4. 输出
   └── 计算位置 → 创建图层 → 复制实体 → 应用变换
```

## 测试方案

- 使用用户提供的.dxf样本文件
- 验证排版结果正确性
- 验证间距符合要求（10mm）
- 验证材料利用率合理
- 验证排版速度可接受

## 后续扩展（暂不实施）

- 批量处理多个文件
- 切割顺序优化
- 保存/加载排版项目
- 材料利用率统计
